<%= form_for(resources, :html => { :class => "#{resource.new_record? ? 'new' : 'edit' }_catalog" }) do |section| %>
  <%= section.hidden_field :type %>

  <%= section.label :title %>
  <%= section.text_field :title %>

  <%= section.submit resource.new_record? ? 'Create' : 'Save' %>
<% end %>