<%= form_for [:cart, Item.new(:quantity => 1, :product => resource)] do |f| %>
  <%= hidden_field_tag :return_to, request.url %>
  <%= f.hidden_field :product_id %>

  <%= f.label :quantity %>
  <%= f.text_field :quantity %>

  <%= submit_tag t('.submit') %>
<% end %>