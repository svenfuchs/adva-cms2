<%= form_for(current_cart, :url => cart_addresses_path, :as => :addresses, :html => { :method => :post }) do |f| %>
  <%= render :partial => 'fields', :locals => { :f => f } %>
  <%= f.submit t('.submit') %>
<% end %>
