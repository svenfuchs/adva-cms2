<%= link_to resource.section.title, [:admin, resource.section.site, resource.section] %>

<div class="<%= resource.type.downcase %>">
  <h2><%= resource.title %></h2>
  <p><%= resource.body %></p>
</div>