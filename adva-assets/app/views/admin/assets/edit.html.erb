<h3><%= t(:'.') %></h3>

<%= simple_form_for @asset, :url => admin_site_assets_path(site, @asset), :method => :post do |f| %>

  <%= f.input :title %>
  <%= f.input :description, :as => :text %>
  <%= f.file_field :file %>

  <%= f.button :submit %>
<% end %>
