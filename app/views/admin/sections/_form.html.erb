<%= form_for(section, :url => url) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  
  <%= f.label :type %>
  <%= f.select :type, section_types_for_select %>
  
  <%= f.submit section.new_record? ? 'Create' : 'Save' %>
<% end %>