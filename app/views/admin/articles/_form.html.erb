<%= form_for(resource) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  
  <%= f.label :body %>
  <%= f.text_area :body %>
  
  <%= f.submit article.new_record? ? 'Create' : 'Save' %>
<% end %>